# pyweb-ssg-project

Исследовательский статический сайт на MkDocs Material.

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
make build
```

После настройки CI добавьте сюда бейдж статуса и URL опубликованных сайтов.

